# الأهداف طويلة المدى ووصف المهمات بالصوت

## الاستخدام

- أضف مهمة، ثم افتح «تفاصيل» لتكتب وصفها أو تمليه بالعربية.
- فعّل «تثبيت كهدف طويل المدى» ليظهر الهدف في قسم «أهدافي المثبّتة» عبر الأيام.
- سجّل ملاحظة عن تقدمك، أو عدّل النسبة من 0 إلى 100، ثم احفظ. يمكن تسجيل ملاحظة دون تغيير النسبة.
- 100% تكمل الهدف. تجد الأهداف المكتملة عبر «عرض المكتملة»، ويمكن فتحها وتخفيض النسبة لمواصلة العمل.
- إلغاء التثبيت يعيد المهمة إلى موعدها الأصلي ويحافظ على الوصف والسجل. تثبيت مهمة متكررة يوقف تكرارها.

## قرار التنفيذ

التطبيق React/Vite ويخزن البيانات في IndexedDB محليًا. أضيفت حقول `description` و`is_pinned` و`progress` و`progress_entries` إلى كتل المهمات؛ الحقول اختيارية للبيانات القديمة. سجل التقدم يحتوي النص والنسبة والطابع الزمني. قراءة المهمة وتعديلها تتم في معاملة واحدة لمنع ضياع سجلها أثناء التعديلات المتزامنة.

الأهداف لا تدخل ضمن عداد مهمات اليوم أو المتأخرات والتقويم اليومي. إكمالها يدخل ضمن إجمالي الإنجازات. التصدير والاستيراد JSON والطباعة يشملان الوصف والسجل؛ سلة المحذوفات تحتفظ بهما.

## الإملاء

يستخدم Web Speech API عند توفره، باللغة `ar-SA`، وفي سياق آمن (HTTPS أو localhost). يعرض النص المؤقت، ويلحق النتائج النهائية بالوصف الموجود، ويتيح مراجعتها قبل الحفظ. لا يسجل التطبيق ملفًا صوتيًا. قد يرسل المتصفح الصوت لخدمة التعرف على الكلام ويحتاج الإنترنت؛ لذلك وُضّح ذلك في الواجهة والإعدادات. يتوقف الاستماع عند إغلاق محرر الوصف.

لا تتساوى المتصفحات في دعم الإملاء، ووجود الواجهة البرمجية لا يضمن توفر الخدمة أو اللغة. تظهر رسالة عند رفض الميكروفون أو فشل الخدمة، ويتاح استخدام إملاء لوحة المفاتيح. لم يُختبر التعرف الفعلي على صوت بشري أثناء التطوير، ويحتاج تجربة على جهاز المستخدم ومتصفحه.

المراجع: [SpeechRecognition](https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition)، [Using the Web Speech API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API/Using_the_Web_Speech_API).

## التحقق

`npm test` يشغّل اختبارات التخزين بمعزل عن بيانات المستخدم باستخدام fake-indexeddb؛ تشمل استمرار الأهداف عبر التواريخ، التزامن، الحدود، السجل، الإكمال، إعادة الفتح، التكرار، السلة والنسخ الاحتياطي. `npm run build` يبني نسخة النشر في docs. اختُبرت واجهة التثبيت والحفظ والتنقل وإعادة التحميل محليًا ببيانات تجريبية منفصلة.
